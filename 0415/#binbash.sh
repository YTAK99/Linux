#!/bin/bash : Shebang(쉬뱅). 이 스크립트를 어떤 프로그램으로 실행할지 지정하는 것
이 코드는 Bash 문법으로 쓴 거니까 Bash로 실행해라


#!
→ “이 파일은 그냥 텍스트가 아니라 실행 파일이다” 
	+ “뒤에 있는 프로그램으로 실행해라” 라는 의미

/bin/bash
→ **Bash 쉘 프로그램 경로.
즉, 이 스크립트를 Bash로 실행하라는 뜻